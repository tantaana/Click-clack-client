import React from 'react';
import AllTitle from '../../Hooks/AllTitle/AllTitle';

const Blog = () => {
    AllTitle('Blog')
    return (
        <div>
            <h2>blog</h2>
        </div>
    );
};

export default Blog;