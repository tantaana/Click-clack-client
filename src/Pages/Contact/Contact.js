import React from 'react';
import AllTitle from '../../Hooks/AllTitle/AllTitle';

const Contact = () => {
    AllTitle('Contact')
    return (
        <div>
            <h2>contact</h2>
        </div>
    );
};

export default Contact;